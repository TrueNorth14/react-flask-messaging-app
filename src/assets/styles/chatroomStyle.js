import ChatRoom from "../../components/ChatRoom/ChatRoom"

const chatRoomStyle = {
    root: {
        display: "flex",
        justifyContent: "center",
        justifyItems: "center",
    },
    chatRoom: {
        maxWidth: "500px",
        minWidth: "300px",
    }
}

export default chatRoomStyle;